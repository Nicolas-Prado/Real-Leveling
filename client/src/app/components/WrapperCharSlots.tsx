'use client'
import { useEffect } from 'react'
import styles from './styles/charSlots.module.scss'

export default function CharSlots({
    children
}: {
    children: React.ReactNode
}){

    useEffect(() => {
        
    })

    return <div className={styles.charSlots}>
        childre
    </div>
}